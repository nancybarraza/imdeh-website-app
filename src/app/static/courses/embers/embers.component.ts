import { Component, OnInit } from '@angular/core';

@Component({
	selector: 'app-embers',
	templateUrl: './embers.component.html',
	styleUrls: ['./embers.component.scss'],
})
export class EmbersComponent implements OnInit {
	constructor() {
		//
	}

	/**
	 * @method ngOnInit
	 * Initializes the component lifecycle.
	 * Loading all the required data to display in the template.
	 * @return {void}
	 */
	public ngOnInit(): void {}
}
